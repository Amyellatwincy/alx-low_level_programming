#include "main.h"

/**
 * _stelen - function to get the length of a string
 * @s: string pointre to passed to this function
 * Return: returns length of the string
 */
int _strlen(char *s)
{
return (strlen(s));
}
